// Configuration values
const configObject = {
    prefix: '.',
};

export enum Colors {
    HAPPY_GREEN = '#3CFC00',
    CAUTION_YELLOW = '#FFD700',
    ERROR_RED = '#FF0000',
};

export default configObject;